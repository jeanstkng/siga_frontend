import { Routes, RouterModule } from '@angular/router';
import { CommitmentLetterComponent } from './commitment-letter.component';

const routes: Routes = [
  {  },
];

export const CommitmentLetterRouting: Routes = [
  {
    path: '',
    component: CommitmentLetterComponent,

    
  },
];